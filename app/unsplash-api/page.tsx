import { getAllPhotos, getOnePhoto, getRandomPhoto } from "@/lib/data/unsplash-data"

export default async function UnsplashApi() {
    // const data = await getAllPhotos()
    // const data = await getOnePhoto(id)
    // const data = await getRandomPhoto()
    // console.log(data)
    return(
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
        Unsplash Page
        
    </main>
    )
}